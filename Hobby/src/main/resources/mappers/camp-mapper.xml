<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="camp">
	
	<!-- 좋아요 수 증가 쿼리 -->
	<insert id="increaseLikeCount" parameterType="String">
	    INSERT INTO camp_likes (facltNm, likes_count) VALUES (#{facltNm}, 1) 
	    ON DUPLICATE KEY UPDATE likes_count = likes_count + 1
	</insert>

	<!-- 좋아요 수 조회 쿼리 -->
	<select id="getLikeCount" resultType="int">
	    SELECT likes_count 
	    FROM camp_likes 
	    WHERE facltNm = #{facltNm}
	</select>
</mapper>